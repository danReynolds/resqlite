## 0.1.3

- Require `resqlite: ^0.7.0`. Compatibility release for the resqlite 0.7.0 bump;
  no functional changes.

## 0.1.2

- Require `resqlite: ^0.6.0`. Compatibility release for the resqlite 0.6.0 bump;
  no functional changes.

## 0.1.1

- Require `resqlite: ^0.5.0`. Compatibility release for the resqlite 0.5.0 bump;
  no functional changes.

## 0.1.0

- Initial resqlite wrapper for SQLite Vector native extension loading.
